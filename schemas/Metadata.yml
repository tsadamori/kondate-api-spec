type: object
required:
  - metadata
properties:
  metadata:
    type: object
    required:
      - limit
      - offset
      - count
    properties:
      limit:
        type: integer
        description: リミット値
        nullable: true
        minimum: 0
      offset:
        type: integer
        description: オフセット値
        minimum: 0
      count:
        type: integer
        description: カウント値
        minimum: 0
